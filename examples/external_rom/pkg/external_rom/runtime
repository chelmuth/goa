runtime | ram: 16M | caps: 1000 | binary: init
+ requires
  + rom | label: input
+ content
  + rom | label: init
  + rom | label: report_rom
  + rom | label: rom_reporter
+ config
  + parent-provides
  | + service CPU
  | + service LOG
  | + service PD
  | + service RM
  | + service ROM
  + start report_rom | caps: 100
  | + resource RAM | quantum: 1M
  | + provides
  | | + service Report
  | | + service ROM
  | + config | verbose: yes
  | + route
  |   + any-service
  |     + parent
  + start rom_reporter | caps: 100
    + resource RAM | quantum: 2M
    + config
    | + rom | label: input
    + route
      + service ROM | label: input
      | + parent | label: input
      + service Report | label: input
      | + child report_rom
      + any-service
        + parent
-
