runtime | ram: 10M | caps: 1000 | binary: init
+ content
  + rom | label: init
  + rom | label: hello
+ config
  + parent-provides
  | + service ROM
  | + service LOG
  | + service CPU
  | + service PD
  + default-route
  | + any-service
  |   + parent
  + start one | caps: 100
  | + binary hello
  | + resource RAM | quantum: 1M
  | + config
  + start two | caps: 100
    + binary hello
    + resource RAM | quantum: 1M
    + config
-
