runtime | ram: 10M | caps: 1000 | binary: init
+ content
  + rom init
  + rom hello
+ config
  + parent-provides
  | + service ROM
  | + service LOG
  | + service CPU
  | + service PD
  + default-route
  | + any-service
  |   + parent
  + start one | caps: 100 | ram: 1M
  | + binary hello
  | + config
  + start two | caps: 100 | ram: 1M
    + binary hello
    + config
-
