runtime | ram: 10M | caps: 1000 | binary: init
+ config
  + parent-provides
  | + service ROM
  | + service LOG
  | + service RM
  | + service CPU
  | + service PD
  + default-route
  | + any-service
  |   + parent
  |   + any-child
  + default | caps: 100
  + start 2-import | caps: 120 | ram: 2M
    + config
      + libc | stdout: /dev/log
      + vfs
        + dir dev
        | + log
        + dir tmp
        | + ram
        | + rom static | label: file_content | binary: no
        + import
          + dir tmp
            + rom x | label: file_content | binary: no
+ content
  + rom 2-import
  + rom vfs.lib.so
  + rom vfs_import.lib.so
  + rom posix.lib.so
  + rom libc.lib.so
  + rom libm.lib.so
  + rom file_content
-
