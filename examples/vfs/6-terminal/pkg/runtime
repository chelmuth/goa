runtime | ram: 16M | caps: 1000 | binary: init
+ requires
  + timer
+ config
  + parent-provides
  | + service ROM
  | + service LOG
  | + service RM
  | + service CPU
  | + service PD
  | + service Timer
  + default-route
  | + any-service
  |   + parent
  |   + any-child
  + default | caps: 100
  + start terminal_crosslink | ram: 4M
  | + provides
  |   + service Terminal
  + start producer | ram: 4M
  | + binary stdin2out
  | + config
  |   + vfs
  |   | + inline source.txt
  |   | | : Hi, this is a terminal test!
  |   | | :
  |   | + dir dev
  |   |   + terminal
  |   + libc | stdin: /source.txt | stdout: /dev/terminal
  + start consumer | ram: 4M
    + binary stdin2out
    + config
      + vfs
      | + dir dev
      |   + log
      |   + terminal
      + libc | stdin: /dev/terminal | stdout: /dev/log
+ content
  + rom | label: vfs.lib.so
  + rom | label: terminal_crosslink
  + rom | label: stdin2out
  + rom | label: posix.lib.so
  + rom | label: libc.lib.so
  + rom | label: libm.lib.so
-
