runtime | ram: 10M | caps: 1000 | binary: init
+ config
  + parent-provides
  | + service ROM
  | + service LOG
  | + service RM
  | + service CPU
  | + service PD
  + default-route
  | + any-service
  |   + parent
  |   + any-child
  + default | caps: 100
  + start report_rom
  | + resource RAM | quantum: 1M
  | + provides
  | | + service Report
  | | + service ROM
  | + config | verbose: yes
  + start fs_query | caps: 120
    + resource RAM | quantum: 2M
    + config
      + vfs
      | + inline README
      | | : Mail me for the README!
      | + inline VERSION
      | | : 2.16
      | + dir config
      |   + inline verbose
      |   | : yes
      |   + dir tcp_ports
      |   + dir udp_ports
      |   | + inline 69
      |   |   + forward | to: 10.0.0.1
      |   + inline dns_servers
      + query | path: / | content: yes | size: yes
      + query | path: /config | content: yes | size: yes
      + query | path: /config/udp_ports | content: yes | size: yes
+ content
  + rom | label: fs_query
  + rom | label: report_rom
  + rom | label: vfs.lib.so
-
