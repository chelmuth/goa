runtime | ram: 16M | caps: 1000 | binary: init
+ requires
  + timer
+ config
  + parent-provides
  | + service ROM
  | + service LOG
  | + service RM
  | + service CPU
  | + service PD
  | + service Timer
  + default-route
  | + any-service
  |   + parent
  |   + any-child
  + default | caps: 100
  + start vfs
  | + resource RAM | quantum: 4M
  | + provides
  | | + service File_system
  | + config
  |   + vfs
  |   | + pipe
  |   |   + fifo named-pipe
  |   + default-policy | root: / | writeable: yes
  + start producer
  | + binary stdin2out
  | + resource RAM | quantum: 4M
  | + config
  |   + vfs
  |   | + inline source.txt
  |   | | : Hi, this is a pipe test!
  |   | | :
  |   | + dir pipe
  |   |   + fs
  |   + libc | stdin: /source.txt | stdout: /pipe/named-pipe
  + start consumer
    + binary stdin2out
    + resource RAM | quantum: 4M
    + config
      + vfs
      | + dir dev
      | | + log
      | + dir pipe
      |   + fs
      + libc | stdin: /pipe/named-pipe | stdout: /dev/log
+ content
  + rom | label: vfs
  + rom | label: vfs.lib.so
  + rom | label: vfs_pipe.lib.so
  + rom | label: stdin2out
  + rom | label: posix.lib.so
  + rom | label: libc.lib.so
  + rom | label: libm.lib.so
-
